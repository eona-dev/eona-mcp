{
  "schema_version": 1,
  "product": "eona-cli",
  "purpose": "runtime_compatibility",
  "compatibility": {
    "min_version": "0.1.1",
    "query_version": 1,
    "required_commands": [
      "add",
      "query",
      "session sources",
      "session reset",
      "session refresh",
      "status"
    ]
  },
  "notes": [
    "eona-cli may self-upgrade as a sibling runtime under the EONA family root.",
    "eona-mcp should validate runtime compatibility instead of enforcing a fixed artifact hash at startup."
  ]
}
