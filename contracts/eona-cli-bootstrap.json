{
  "schema_version": 1,
  "product": "eona-cli",
  "purpose": "bootstrap_install",
  "version": "0.1.2",
  "artifacts": {
    "macos_arm64": {
      "path": "releases/0.1.2/eona-cli/eona-macos-arm64-0.1.2.tar.gz",
      "sha256": "PINNED_BY_RELEASE_WORKFLOW"
    }
  },
  "notes": [
    "Use this only when installing or repairing a missing eona-cli sibling runtime.",
    "Runtime startup should use eona-cli-dependency.json for compatibility checks because eona-cli may upgrade itself."
  ]
}
